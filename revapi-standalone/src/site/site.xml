<?xml version="1.0" encoding="UTF-8"?>
<project name="Revapi Standalone">
  <bannerLeft>
    <name>${project.name}</name>
    <href>${project.url}</href>
  </bannerLeft>

  <custom>
    <reflowSkin>
      <slogan>${project.description}</slogan>
      <bottomDescription>${project.name}</bottomDescription>
    </reflowSkin>
  </custom>
</project>
